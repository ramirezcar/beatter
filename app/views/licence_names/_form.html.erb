<%= form_with(model: licence_name) do |form| %>
  <% if licence_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(licence_name.errors.count, "error") %> prohibited this licence_name from being saved:</h2>

      <ul>
        <% licence_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
